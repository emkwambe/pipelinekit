name: salesforce_to_snowflake
version: "1.0.0"
config-version: 2

profile: salesforce_to_snowflake

model-paths: ["models"]

models:
  salesforce_to_snowflake:
    staging:
      +materialized: view
    core:
      +materialized: table
