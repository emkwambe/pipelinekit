# ADR-000: Test Fixture Decision

**Status:** Accepted

## Context

A committed fixture ADR so `ADRReader` tests are self-contained and do not
depend on the gitignored `docs/decisions/` directory.

## Decision

Adopt a committed fixture ADR under `tests/fixtures/decisions/` for tests.
